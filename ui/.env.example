# Optional: when the UI hostname differs from the FastAPI host (e.g. UI on
# mirror.smart-mirror.tech, API on smart-mirror.tech). Copy to `.env` in this
# folder and rebuild (`npm run build`). No trailing slash.
# VITE_BACKEND_ORIGIN=https://smart-mirror.tech
