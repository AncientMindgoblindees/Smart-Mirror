{
  "name": "smart-mirror-d1-sync-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:migrate": "wrangler d1 migrations apply MIRROR_DB"
  },
  "devDependencies": {
    "wrangler": "^4.12.0"
  }
}
